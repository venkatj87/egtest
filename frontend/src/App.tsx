import { Outlet } from 'react-router-dom';

// Create env variable
// Add some styles
// create git hub repository
// write documentation
// Add comments what where neccessary

function App() {
  return (
    <div className="App">
      <Outlet />
    </div>
  );
}

export default App;
